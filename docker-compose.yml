version: '3'
services:
    orch-front:
        build: ../orchestrator-frontend/.
    orch-auth-service:
        build: ../orchestrator-authentication-service/.
    nginx:
        build: /.
        ports:
            - 80:80